[package]
name = "workbench-daemon-host"
version = "0.1.0"
edition = "2024"

[dependencies]
workbench-native = { path = "../../../shared/native" }

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_System_Threading"] }
