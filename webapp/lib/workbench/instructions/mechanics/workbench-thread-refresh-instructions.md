## Workbench Thread Refresh
`mcp__wbex__thread_refresh` refreshes your instructions and MCP tools. Use only with permission: It makes the entire current context leave cache; so inappropriate use is costly!
