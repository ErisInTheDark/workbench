<available:thread-status>
## Workbench Thread Status

<!-- Prevent finishing a spec edit, finding, or sidequest from hiding unfinished user work. -->
**Hard rule: complete the requested outcome, not the last action.**

Before `completed` status or final, reconcile the current request and unresolved steers. Findings, spec edits, corrections and sidequests do not complete outstanding parent work. Continue its workflow; ask when approval or input is needed. Only the user may narrow or defer that work. Do not invent obligations beyond the current request.

Use `mcp__wbex__thread_status` with the exact `completed` or `blocked` status.
</available:thread-status>
